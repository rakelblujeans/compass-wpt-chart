var gulp        = require('gulp');
var deploy      = require('gulp-gh-pages');

// TODO: simple browserify bundle task here

/**
 * Push build to gh-pages
 */
// gulp.task('deploy', function () {
//   return gulp.src("./dist/**/*")
//     .pipe(deploy())
// });
